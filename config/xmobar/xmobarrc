Config {
       font = "xft:Iosevka:pixelsize=14",
       fgColor = "#000000"
       bgColor = "#ffffff"
       commands = [
                -- Ithaca, NY
                Run Weather "KITH" ["-t"," <tempF>F"] 36000,
                Run Date "%a %b %_d %l:%M" "date" 10,
                Run StdinReader
                ]
       , sepChar = "%"
       , alignSep = "}{"
       , template = "%StdinReader% }{ %date%  |  %KITH%   "
       }

